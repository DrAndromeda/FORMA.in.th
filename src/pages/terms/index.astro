---
import LegalPage from '../../templates/LegalPage.astro';
import { legalEn } from '../../content/legal/en';

const p = legalEn.terms;
---

<LegalPage
  path="/terms/"
  navLabel="Terms of Service"
  title={p.title}
  description={p.description}
  h1={p.h1}
  updated={p.updated}
  sections={p.body}
/>
